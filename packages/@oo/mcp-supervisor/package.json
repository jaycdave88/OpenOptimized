{
  "name": "@oo/mcp-supervisor",
  "version": "0.1.0",
  "private": true,
  "description": "Spawn, health-check, and restart bundled MCP servers for OpenOptimized.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "execa": "^9.5.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
