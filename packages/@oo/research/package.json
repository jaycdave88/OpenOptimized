{
  "name": "@oo/research",
  "version": "0.1.0",
  "private": true,
  "description": "Wrapper around the autoresearch Python sidecar: start/stop runs, stream stdout, surface artifacts.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
